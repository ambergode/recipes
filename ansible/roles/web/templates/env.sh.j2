. pvenv/bin/activate
export SISEPUEDE_ENV={{ ssp_env }}
export SISEPUEDE_LOGS="/var/log/{{ ssp_name }}/"
# Secrets.sh must be created manually on the host. It should contain
# export SECRET_KEY=XXX
. ../secrets.sh